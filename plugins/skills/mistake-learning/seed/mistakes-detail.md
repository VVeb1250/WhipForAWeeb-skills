# Mistakes Detail

> On-demand root-cause store. NOT auto-loaded. Referenced from mistakes-index.md via `[id]`.
> Load only when a pattern actually fires (debugging). Index keeps trigger→fix; full story lives here.
> LOW-severity mistakes (seen once) go straight here, never into the index.

---

(none yet)
